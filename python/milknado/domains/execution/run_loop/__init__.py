"""Run-loop helper modules. The orchestrator (source's `__init__.py`) is dropped
per spec D-4 — the daemon scheduler will replace it in sub-PR #2. Helpers
remain here so executor.py keeps using them as-is.
"""
