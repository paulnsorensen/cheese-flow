# DAG_HEALTH

Design-quality scoreboard for the cheese-flow agent + DAG ecosystem. Every
row scored 0–100. The ralph stops when every row is >= 90.

Last updated: 2026-04-26, iteration 20.

## Flows (from Quintessential Agentic Flows for Cheese-Flow.md)

| Flow | Score | Notes |
|---|---|---|
| 1. Spec-First (Cook → Cut → Press → Age) | 85 | `/fromage` rewritten with a concrete six-step **Execution protocol** mirroring the `/debug` gold-standard: corrects the prior canonical-vs-agent role mismatch (Cook agent = implement, Cut agent = TDD red-tests, Press agent = adversarial post-impl) by routing the actual sub-agent dispatch as `Cut → Cook → Press → Age` (TDD discipline) while keeping the canonical `Cook → Cut → Press → Age` pipeline-phase label and an explicit phase→agent mapping table in the Flow section. Step 1 unifies spec resolve + validate (Approach + Quality gates) + classify (>= 4 atoms → `/fromagerie`, linear backlog → `/incremental`) + slug derivation + plan write to `$TMPDIR/fromage-<slug>-plan.md` + `AskUserQuestion` gate. Steps 2-4 are explicit `Agent(subagent_type=cut|cook|press, ...)` invocations with full prompts pinning per-stage scope (Cut: pin only spec-named obligations, no invented edge cases; Cook: edit only plan-named files, no test edits; Press: tests-only post-impl, score 0-100). Step 5 is `Skill(skill="age", args="--no-fix --scope ...")` to keep the Fromage orchestrator in control of the fix loop. Step 6 has three exit branches (success / impl-only fix loop back to Cook / spec-invalidation halt to `/mold`) plus the three-loop convergence cap. Hand-off contract enumerates the exact $TMPDIR paths for every artifact. Cross-harness portability section names Codex / Copilot CLI / Cursor degradation paths. Deferred section limited to blocked-on-src items. |
| 2. Exploration (Culture → Cook iterative → Cut → Press → Age) | 85 | `/explore` rewritten with a concrete eight-step **Execution protocol** mirroring the `/debug`, `/fromage`, and `/pr-finish` gold-standard. Adds an explicit phase→agent mapping table that resolves the canonical-vs-agent role layering — the canonical `Culture → Cook iterative → Cut → Press → Age` becomes the loop (`Culture → Cook` × up to 3) → approach lock → `Cut → Cook → Press → Age`, with Cook used twice (propose-only during the loop, standard impl mode after the lock). Step 1 unifies classify + redirect + slug derivation + AskUserQuestion gate. Step 2 is the Culture↔Cook loop with explicit `Agent(subagent_type=culture)` and `Agent(subagent_type=cook, ...propose-only...)` invocations per cycle, cycle-counter tracking starting at 1, capped at 3, and a Cook-driven branch decision (`advance to lock` vs `loop again` with focusing question). Step 3 is the `AskUserQuestion`-gated approach lock with the three-choice menu (lock / loop again / abort) and a stable `$TMPDIR/explore-<slug>-approach.md` proposal path. Steps 4-6 are explicit `Agent(subagent_type=cut|cook|press, ...)` invocations against the locked approach with full prompts pinning per-stage scope (Cut: pin only locked-approach-named obligations; Cook impl: edit only approach-named files, lock is binding so do not fold rejected alternatives back in; Press: tests-only post-impl with Press's primary attack surface = the locked approach's "unresolved questions" section). Step 7 is `Skill(skill="age", args="--no-fix --scope ...")` so the Explore orchestrator owns the impl-fix loop. Step 8 has three exit branches (success / impl-only fix loop back to Cook / approach-invalidation halt to `/explore` or `/mold`) plus the three-loop convergence cap on Cook → Press → Age. Hand-off contract enumerates every $TMPDIR path with explicit namespace separation (`explore-<slug>-*` for loop artifacts, `fromage-*-<slug>` for post-lock pipeline). Cross-harness portability section names Codex / Copilot CLI / Cursor degradation paths and explicitly calls out that loop-Cook's propose-only constraint comes entirely from the Step 2b prompt. Deferred section lists three blocked-on-src items (compiler propagation, loop counter persistence, first-class cook-propose-only flag). |
| 3. Debug (Culture → Cook → Press → Age) | 85 | `/debug` rewritten with a concrete six-step **Execution protocol** that mirrors the `age` skill's gold-standard pattern: explicit `Agent(subagent_type=...)` invocations for Culture / Cook / Press with full prompts pinning down per-stage scope (Culture's confidence-≥-50 root-cause gate; Cook's "edit only files Culture cited" hard constraint; Press's "regression test that would have failed pre-fix" obligation), explicit `Skill(skill="age", args="--no-fix --scope ...")` invocation for the Age step (the `--no-fix` mode prevents `/age` from double-spawning its own Press loop while the Debug orchestrator owns the fix loop), `AskUserQuestion`-gated step-1 confirmation and step-6 fix-loop continuation, three-loop convergence cap with explicit loop-counter tracking starting at 1, and an explicit `<slug>` derivation rule that ties every $TMPDIR path together. Hand-off contract now enumerates the exact `$TMPDIR/fromage-{culture,cook,press}-<slug>.md` + `$TMPDIR/age-<slug>.md` paths (matched against agents/*.md.eta source). Cross-harness portability section calls out Codex (no Agent tool → sequential turns), Copilot CLI (no parallel-spawn — already matches sequential handoff), and Cursor (no per-agent allowlist → prompt-only enforcement on Culture). Deferred section trimmed to truly blocked-on-src items (compiler does not propagate disallowedTools, no durable loop-counter file). |
| 4. PR-Finish (Culture → Cut → Press → Age) | 85 | `/pr-finish` rewritten with a concrete seven-step **Execution protocol** mirroring the `/debug` and `/fromage` gold-standard. Surfaces and corrects a long-standing canonical-vs-agent role mismatch — the canonical doc labels the flow `Culture → Cut → Press → Age` with Cook-skipped semantics where "Press executes the task list," but our `press.md.eta` is tests-only post-impl. The rewrite re-explains "Cook skipped" as "no fresh planning Cook" (the architectural decision lives in the existing PR description) while making the actual sub-agent dispatch order `Culture → Cut → Cook → Press → Age` explicit via a phase→agent mapping table; the cook sub-agent IS dispatched at step 4 to do the remaining implementation work, and the canonical-doc Cut/Press approval gate shifts to between Cut and Cook under agent semantics. Step 1 unifies PR/branch resolve + classify + slug derivation (`pr-<number>` or kebab branch) + plan stub write to `$TMPDIR/pr-finish-<slug>-plan.md` + `AskUserQuestion` gate. Steps 2-5 are explicit `Agent(subagent_type=culture|cut|cook|press, ...)` invocations with full prompts pinning per-stage scope (Culture: confidence-≥-50 divergence-assessment gate plus required goal/diff-surface/CI-failures/open-threads/linked-spec deliverables; Cut: pin only PR/spec/thread-named obligations + each failing CI check is a contract claim; Cook: edit only Culture's diff surface, no rebase/force-push; Press: tests-only post-impl, score 0-100). Approval gate fires *inside* step 3 with three-choice menu (confirm/edit/abort). Step 6 is `Skill(skill="age", args="--no-fix --scope ...")` so the PR-Finish orchestrator owns the fix loop. Step 7 has three exit branches (success / impl-only fix loop back to Cook / divergent-PR halt to `/mold`) plus the three-loop convergence cap. Hand-off contract enumerates every $TMPDIR path. Cross-harness portability section names Codex / Copilot CLI (gh-pr surface is Copilot-native) / Cursor degradation paths. Deferred section limited to blocked-on-src items. |
| 5. Review (Culture → Age → loop) | 85 | `/age` rewritten with a concrete seven-step **Execution protocol** mirroring the `/debug`, `/fromage`, `/pr-finish`, `/explore`, `/incremental`, and `/culture` gold-standard. Surfaces and corrects the canonical-vs-agent role mismatch — the canonical doc labels the loop `Press → Age (re-review)` with Press-as-fixer semantics, but our `press.md.eta` is tests-only post-impl. The rewrite re-explains the fix loop as `Cook (production fix) → Press (regression tests, optional) → age skill (re-review)` via an explicit phase→agent mapping table; Cook is the primary fixer because most Age findings (bugs, complexity, leaks, dead code, spec drift) are production-code issues that Press's tests-only Permission Contract forbids; Press is dispatched only when an Age finding is `SPEC_MISSING` shaped. Step 1 unifies target resolve + classify (redirect to `/debug` for bugs, `/pr-finish` for stalled PRs, `/mold`+`/fromage` for fresh features, `/culture`+`/explore` for no-diff exploration) + slug derivation (`pr-<number>` for PRs, kebab branch for branches, last path component for paths) + `AskUserQuestion` gate. Step 2 is the Culture pre-pass with explicit `Agent(subagent_type=culture, ...)` invocation, an explicit `$TMPDIR/age-<slug>-context.md` path override that namespace-isolates the Review brief from any concurrent `/debug` or `/fromage` Culture run on the same target, and required goal/diff-surface/CI-failures/open-threads/linked-spec/prior-Age-reports deliverables plus a confidence-≥-50 reviewability gate. Step 3 invokes the `age` skill (which spawns the six dimensions in parallel and merges with history modifiers) and writes the unified report to `$TMPDIR/age-<slug>.md`. Step 4 is the fix-loop branch decision with three `AskUserQuestion` choices (confirm fix / abort / spec-invalidation halt). Step 5 is `Agent(subagent_type=cook, ...)` scoped to Age-cited files only with full prompt pinning per-loop scope (read both prior reports first; edit only files Age cited at score >= 50; no test edits — those go to Press at step 6). Step 6 is the optional `Agent(subagent_type=press, ...)` dispatch — invoked only if any Age finding is `SPEC_MISSING` shaped (regression-test missing) — with full prompt scoping the press attack surface to the missing-test findings only. Step 7 is the `Skill(skill="age", args="--no-fix --scope ...")` re-review for delta-only output, with three exit branches (success / loop again with counter increment / three-loop convergence cap halt) plus a spec-invalidation override that halts regardless of loop count. Hand-off contract enumerates every $TMPDIR path with explicit namespace separation (`age-<slug>-context.md` for Culture brief override, `age-<slug>.md` for initial review, `age-<slug>-loop-<N>.md` for re-review per loop, `fromage-cook-<slug>.md` and `fromage-press-<slug>.md` for the canonical writer seams). Cross-harness portability section names Codex (Agent → sequential turns; age skill's six-dimension parallel-spawn → sequential on Codex) / Copilot CLI (sequential outer flow native, age-skill parallelism degrades) / Cursor (no allowlist → prompt-only enforcement on Culture + six dimensions) degradation paths. Deferred section lists three blocked-on-src items (compiler propagation, loop counter persistence, first-class `--classify` flag on the age skill that returns `production_fix` / `regression_test_only` buckets to remove the orchestrator-side prompt parsing). |
| 6. Incremental (Cook → Cut → Press → Age × N) | 85 | `/incremental` rewritten with a concrete three-step **Execution protocol** mirroring the `/debug`, `/fromage`, `/pr-finish`, and `/explore` gold-standard. Adds an explicit phase→agent mapping table that resolves the canonical-vs-agent role layering — the canonical `[Cook task-N → Cut → Press → Age] × N` becomes per task `[orchestrator: write task-N plan] → Cut → Cook → Press → Age` under agent semantics (Cook = production-code writer, Cut = TDD red tests, Press = adversarial post-impl), with Cook-as-planner replaced by an orchestrator-side per-task plan write so Cook is dispatched only once per task in its standard role. Step 1 unifies spec resolve + classify + redirect (four sibling flows) + slug derivation + state-file init or resume + `AskUserQuestion` gate. State-file shape is now spelled out as concrete JSON with task index, status (`pending`/`in-progress`/`done`/`failed`/`skipped`), `fix_loop_attempts` counter, plan-path pointer, and spec-line range. Step 2 is the per-task pipeline as an explicit seven-substep loop (2a plan, 2b Cut, 2c Cook, 2d Press, 2e Age, 2f fix-loop branch decision, 2g cross-task hand-off) with full `Agent(subagent_type=cut\|cook\|press, ...)` prompts pinning per-task scope (Cut: pin only task-N's named obligations, do not pull work forward from later tasks; Cook: do NOT touch files reserved for tasks > N; Press: scope attack surface to task-N's diff, do not chase prior-task regressions; Age: scoped to task-N's net diff). Step 2e is `Skill(skill="age", args="--no-fix --scope ...")` so the Incremental orchestrator owns the per-task fix loop. Step 2f has four branches (success → mark `done` + advance / impl-only fix loop / plan-invalidation halt to `/mold` / three-loop cap → mark `failed`). Step 3 returns cumulative summary on backlog complete. Hand-off contract enumerates every per-task $TMPDIR path with `incremental-<slug>-task-<N>-*` namespace separation. Cross-harness portability section names Codex / Copilot CLI / Cursor degradation paths and explicitly calls out that the state file is harness-agnostic JSON. Deferred section lists three blocked-on-src items (compiler propagation, per-task scope flag, task-cycle counter — though the state file mostly closes that gap). |
| 7. Learn (Culture only) | 85 | `/culture` rewritten with a concrete four-step **Execution protocol** mirroring the `/debug`, `/fromage`, `/pr-finish`, `/explore`, and `/incremental` gold-standard. Adds an explicit phase→agent mapping table that resolves the canonical-vs-agent role layering — the canonical `Culture (only)` (or optional `Culture → Cook (draft explanation)`) becomes `Culture (only)` under agent semantics, with the canonical-doc "Cook draft explanation" folded into an orchestrator-side aggregation step (since cook's production-code-only Permission Contract is incompatible with docs-only output). The Learn flow is uniquely the only flow whose path is a single sub-agent. Step 1 unifies classify + redirect (eight sibling flows: `/debug` for bugs, `/explore` for fuzzy + build, `/mold` for spec-only, `/fromage` for known specs, `/pr-finish` for branches, `/briesearch` for external libs, `/age` for review) + slug derivation + `AskUserQuestion` gate. Step 2 is the per-cycle `Agent(subagent_type=culture, ...)` invocation with cycle-counter tracking starting at 1, capped at 3, full prompt pinning per-cycle scope (deep understanding for a human reader, NOT problem diagnosis; the report IS the deliverable since there is no downstream Cook/Cut/Press/Age; required findings: 3-7 essential entry points, execution flow, architectural pattern to internalize, "if you understand this, you can do X" paragraph, open sub-questions for next cycle), and an explicit `$TMPDIR/learn-<slug>-culture-c<N>.md` path override that namespace-isolates Learn artifacts from any concurrent `/explore` or `/debug` run on the same topic. Step 3 reads all completed cycle reports, composes an aggregated learn summary at `$TMPDIR/learn-<slug>-summary.md`, then offers `AskUserQuestion` with three branches (understanding sufficient → exit / loop another aspect with focusing question — capped at 3 cycles / discovery → halt + redirect to `/explore` / `/mold` / `/fromage` / `/debug` / `/pr-finish`). Step 4 returns the structured learn summary as the canonical artifact with explicit "NEVER spawn cook/cut/press/age" and "NEVER recommend `/culture` recursively" guardrails. Hand-off contract enumerates every per-cycle $TMPDIR path. Cross-harness portability section names Codex (cycles → sequential turns) / Copilot CLI (sequential native) / Cursor (no allowlist → prompt-only Permission Contract on culture) degradation paths and explicitly calls out that the Learn flow is uniquely exposed to the compiler-propagation gap because Culture is its only stage (no downstream agent could catch a Culture-side production write). Deferred section limited to two blocked-on-src items (compiler propagation, cycle-counter persistence). |

## Agents (agents/*.md.eta)

| Agent | Score | Notes |
|---|---|---|
| culture | 75 | Output contract is structured and tight; explicit Permission Contract block now declares read-only-on-production with the `$TMPDIR/fromage-culture-<slug>.md` write carve-out and a cross-harness fallback note; still missing flow-entry guidance for Debug vs Learn vs Exploration. |
| cook | 80 | Charter rewritten as production-code-only writer (tests forbidden — Cut owns pre-impl, press owns post-impl); Permission Contract block declares writer-side scope with explicit `**NO**` rows for test-file edits, build-config tweaks, mutating git, and sub-agents; structured-summary compaction seam at `$TMPDIR/fromage-cook-<slug>.md` with max-1500-char return matches cut/press; per-step confidence column kept and surfaced in the summary table; source frontmatter tightened to disallow `NotebookEdit, WebSearch, WebFetch`; cross-harness fallback note added. |
| cut | 80 | Charter rewritten as pure TDD contract author (no standalone-adversarial mode → no more overlap with press); confidence-scoring vestige dropped (tests ARE the contract — there is nothing to score); Permission Contract block declares tests-only writes with explicit `**NO**` rows for production edits, build/CI changes, commits, and sub-agents; `Contract Definition Strategy` replaces adversarial priorities and is anchored to *named* plan obligations only (adversarial exploration is explicitly punted to press); structured-summary file pattern at `$TMPDIR/fromage-cut-<slug>.md` with a max-1500-char return matches press's seam; cross-harness fallback note added. |
| press | 80 | Charter rewritten as post-implementation adversarial tester (description tightened with "post-implementation" qualifier); stale `roquefort-wrecker` redirect replaced with explicit "for pre-implementation contract definition (TDD red tests before Cook), use cut instead" pointer in both the description and the pipeline-phase note; writer-side `## Permission Contract` block declares tests-only writes with explicit `**NO**` rows for production edits, build-config / lockfile / CI silencing, mutating git, and sub-agents; structured-summary compaction seam tightened from max-2000 → max-1500 chars to match the cut/cook seam; source frontmatter tightened to disallow `NotebookEdit, WebSearch, WebFetch`; redundant "What You Don't Do" section trimmed (permissions now covered by the Contract; the surviving "Scope Discipline" rump captures co-agent role boundaries — Age for quality, age-spec for compliance, Cook for fixes); cross-harness fallback note added. |
| age-safety | 80 | Charter, classify→evidence→score protocol, and disjoint-with-siblings note all present; Permission Contract block now declares strict read-only with `Edit, Write, NotebookEdit` in disallowedTools; minor: no explicit Sliced-Bread cross-link for boundary bugs. |
| age-arch | 80 | Strong measurement protocol with cheez-search/tilth grounding; nesting-depth ladder is concrete; Sliced Bread coverage explicit; Permission Contract block + tightened disallowedTools (Edit, Write, NotebookEdit). |
| age-encap | 80 | Sliced Bread rules embedded; cheez-search "callers" + tilth_deps grounding; classification table covers the 5 leak types; Permission Contract block + tightened disallowedTools (Edit, Write, NotebookEdit). |
| age-yagni | 80 | Justification check protocol is concrete; AI_NOISE deletion fix-it-yourself rule is good; spec-cross-reference covered; Permission Contract block now codifies the < 5-line annotate-with-fix-it-yourself ceiling and the self-check rule. |
| age-history | 90 | Modifier-only contract is tight; one-call helper invocation now points at the real `python/tools/git_file_risk.py` (stdlib-only, plain `python3`) with a fall-through to a `git-file-risk` PATH shim; Permission Contract block now enumerates the helper-script carve-out vs the strict no-write/no-LSP/no-web posture. |
| age-spec | 75 | Spec-first protocol with cheez-search verification; Permission Contract block + tightened disallowedTools (Edit, Write, NotebookEdit); missing "no spec found" fast-exit emphasis in summary. |

## Cross-cutting principles

| Principle | Score | Notes |
|---|---|---|
| Permission model per stage | 75 | Source frontmatter tightened: age-safety / age-arch / age-encap / age-spec disallow `Edit, Write, NotebookEdit`; cook + cut + press now disallow `NotebookEdit, WebSearch, WebFetch`. Writer-side Permission Contract blocks now embedded in **all three** writers — cook (production-code only — tests, build-config, mutating git, sub-agents forbidden), cut (tests-only pre-impl — production code, build-config, commits, sub-agents forbidden), and press (tests-only post-impl — production code, build-config silencing, mutating git, sub-agents forbidden) — each with positive-allowed action tables, explicit `**NO**` rows, and cross-harness fallback notes. Read-only `Permission Contract` blocks present in all seven read-only review agents (culture + 6 age-*). Remaining gap: compiler does not yet propagate `disallowedTools`/`permissionMode` into rendered harness frontmatter (blocked-on-src) — the source declaration is forward-compatible but currently a no-op at runtime. |
| Confidence scoring (>= 50 to surface) | 70 | Applied uniformly across the six age-* agents and press; cook + cut + culture do not score at all. |
| Compaction seams (sub-agents return summaries, not narrative) | 75 | All four writer-side / scout agents now emit structured summaries to $TMPDIR + uniformly bounded returns at the 1500-char cap (culture, cut, cook, press all match the same seam: `$TMPDIR/fromage-<phase>-<slug>.md` for the full body, short table with cited file paths only as the orchestrator-visible return). Press's cap was tightened from 2000 → 1500 this iteration to align. age-* return short tables but body lengths drift; basic-agent unscored. |
| Skill-over-tool delegation | 70 | Agents prefer cheez-* skills over raw Read/Grep; basic-agent still uses raw `read/write/bash`; no skill-or-tool routing helper is shared. |
| Terse output discipline | 60 | Output formats are explicit but agent prompt bodies still carry redundant restatements (e.g. "What You Don't Do" duplicates description). |
| Stop conditions per phase | 60 | Most agents declare a "wrap-up signal" tool-call cap; culture, age-history, and basic-agent do not. |
| Cross-harness portability | 65 | Tool names + skills are in the canonical Claude vocabulary; Claude-only fields (disallowedTools, permissionMode) are tolerated by the compiler; no inline notes in eta about Codex/Copilot/Cursor approximations. |
| Sliced Bread organization | 75 | `skills/README.md` now formalizes the crust/internals convention: `SKILL.md` is the public API loaded by every harness, `scripts/` and `references/` are internals invoked only by the parent SKILL.md, and a four-step growth pattern is documented (one SKILL.md → extract `scripts/` when bash repeats or needs tests → extract `references/` when prose overflows the 500-line linter cap → stay put otherwise). Anti-patterns enumerated (premature `scripts/`, cross-skill internal imports, helper code wedged into fenced markdown). Cross-harness contract spelled out (skills are the only fully isomorphic surface; adapters copy `scripts/`+`references/` as opaque assets). Remaining gap: rule is doc-only — the lint-skills validator does not yet enforce "no path-references-into-other-skills/scripts" structurally; agent-side organization (`agents/*.md.eta` is currently flat with no documented split between facade and supporting prompt fragments) also unscored. |
| Deterministic Python tooling | 80 | `python/tools/` bootstrapped with `git_file_risk.py` (stdlib-only, batch JSON output) plus 21 pytest cases in `tests/python/tools/test_git_file_risk.py` covering `humanize_staleness`, the three git probes, the aggregate `risk_for`, and CLI behaviour. The age-history agent is wired to call it in the same iteration — no orphan tool. Future tools (eta-frontmatter validator, harness translation matrix) still pending. |

## Iteration log

- Iteration 1: bootstrap — initial scoreboard with honest low-biased scores.
- Iteration 2: add `commands/debug.md` (Flow 3 entry point) — Culture→Cook→Press→Age with explicit Cut-skip, per-stage permission contract, evaluator-optimizer fix loop, and stop-condition cap.
- Iteration 3: add `commands/pr-finish.md` (Flow 4 entry point) — Culture→Cut→Press→Age with explicit Cook-skip rationale, AskUserQuestion-gated approval between Cut and Press, divergent-PR redirect to `/mold`, and three-loop Press→Age fix cap.
- Iteration 4: add `commands/incremental.md` (Flow 6 entry point) — per-task Cook→Cut→Press→Age × N with Culture folded into Cook for minimal per-task grounding, JSON state file with resume semantics, three-loop Press→Age cap per task, parallel-shaped-backlog redirect to `/fromagerie`, and per-task scope constraints in every stage row.
- Iteration 5: add `commands/explore.md` (Flow 2 entry point) — Culture↔Cook iterative loop with three-cycle cost-control cap, AskUserQuestion-gated approach lock as the hard commit boundary (lock/loop/abort), mandatory ≥2-alternatives + "Do nothing" rule on Cook, approach-invalidation halt path when Age findings undermine the locked approach, and per-stage no-production-write invariants on Culture and Cook.
- Iteration 6: add `commands/fromage.md` (Flow 1 entry point) — Cook → Cut → Press → Age with Culture folded into Cook's light pre-pass, hard spec-validation gate (Approach + Quality gates required), parallel-atoms classifier redirecting to `/fromagerie` (>= 4 non-overlapping units) and linear-backlog redirect to `/incremental`, three-loop Press → Age cap, and spec-invalidation halt that recommends `/mold` when Age challenges the spec itself.
- Iteration 7: rewrite `commands/age.md` as Flow 5 (Review) entry point — Culture pre-pass with structured brief, per-stage permission table (Culture+Age read-only-on-production, Press the only writer), `AskUserQuestion`-gated Press fix loop scoped to Age-cited files only, three-loop convergence cap, `--no-fix` flag for embedded use inside other flows, spec-invalidation halt path recommending `/mold` or `/explore`, and dual-role framing as both standalone Flow 5 and reusable review primitive.
- Iteration 8: bootstrap `python/tools/` with `git_file_risk.py` (stdlib-only, batch JSON output) and 21 pytest cases covering staleness humanization, the three git probes, the `risk_for` aggregator, and the CLI; rewire `agents/age-history.md.eta` to invoke `python3 python/tools/git_file_risk.py …` (with the `git-file-risk` PATH shim as a fallback) and add the cross-harness portability note. Closes the orphaned-helper gap and lifts the lowest scoreboard row from 25 → 80.
- Iteration 9: tighten the source-frontmatter `disallowedTools` for the four read-only Age dimensions (safety, arch, encap, spec) to add `Write`, matching age-history's strict exemplar; embed a uniform `## Permission Contract` block in all seven read-only review agents (culture + 6 age-*) with a positive-allowed action table, explicit `**NO**` rows for forbidden actions, and a cross-harness fallback note (Codex / Copilot CLI / Cursor honor the prompt contract since they have no per-agent disallowedTools surface). age-yagni's contract codifies the annotate-with-fix-it-yourself ceiling (< 5 lines, score >= 50, category in {DEAD_CODE, AI_NOISE}) plus a self-check rule. Lifts the lowest scoreboard row from 30 → 60. Design notes in `notes/iter-9-permission-contracts.md`.
- Iteration 10: rewrite `agents/cut.md.eta` as pure TDD contract author — drop the standalone-adversarial mode that duplicated press, drop the "Guilty Until Proven Innocent" framing (that is press's identity), drop confidence scoring (Cut produces tests, not findings — the test IS the contract), replace "Adversarial Testing Strategy" with "Contract Definition Strategy" anchored to the plan's *named* obligations only (no inventing edge cases — that's press post-impl), embed a tests-only `## Permission Contract` block with explicit `**NO**` rows for production edits / build-config / commits / sub-agents, add the structured-summary compaction seam (`$TMPDIR/fromage-cut-<slug>.md` + max-1500-char return matching press's pattern), and tighten source frontmatter to disallow `NotebookEdit, WebSearch, WebFetch`. Lifts cut 55 → 80, press 55 → 65 (overlap resolved from cut's side; press's stale roquefort-wrecker pointer noted as the next step), and Compaction seams 55 → 60.
- Iteration 11: formalize Sliced Bread organization for `skills/` in `skills/README.md` — name `SKILL.md` as the crust (public API loaded by every harness), categorize `scripts/`/`references/` as internals invoked only by the parent SKILL.md, document the four-step organic growth pattern (one SKILL.md → extract `scripts/` when bash blocks repeat, exceed ~10 lines, or need pytest coverage → extract `references/` when prose overflows the 500-line linter cap → otherwise stay put), add the cross-skill no-path-into-internals rule (delegate via the crust by skill name only), enumerate anti-patterns (premature `scripts/`, cross-skill internal imports, helper code stuffed into fenced markdown, `references/` as a dumping ground), and pin the cross-harness contract (skills are the only fully isomorphic surface; harness adapters copy `scripts/`+`references/` as opaque assets when emitting per-harness bundles). Lifts Sliced Bread organization 50 → 75. Doc-only change; build green.
- Iteration 12: rewrite `agents/cook.md.eta` as production-code-only writer — embed a writer-side `## Permission Contract` block (production edits allowed; test-file edits forbidden because Cut owns pre-impl tests and press owns post-impl tests; build-config tweaks, mutating git, and sub-agents all explicitly forbidden), add the structured-summary compaction seam at `$TMPDIR/fromage-cook-<slug>.md` with a max-1500-char return matching the cut/press pattern, surface per-step confidence in the summary table (already in the detailed report), tighten source frontmatter to disallow `NotebookEdit, WebSearch, WebFetch`, and add the cross-harness fallback note. Drops the redundant "What You Don't Do" section now subsumed by the Permission Contract; preserves the Quality Gates and Scope Discipline guidance. Lifts cook 60 → 80, Permission model per stage 60 → 70, Compaction seams 60 → 70.
- Iteration 13: rewrite `agents/press.md.eta` as post-implementation tests-only writer — replace stale `roquefort-wrecker` pointer (in both the description and pipeline-phase note) with explicit "for pre-implementation contract definition (TDD red tests before Cook), use cut instead" redirect that mirrors cut's redirect-from-the-other-side; embed a tests-only writer-side `## Permission Contract` block with explicit `**NO**` rows for production edits / build-config or CI silencing / mutating git / sub-agents (mirrors the cook+cut pattern, completing the writer-trio); tighten source frontmatter to disallow `NotebookEdit, WebSearch, WebFetch` (matching cook+cut); align summary cap from max-2000 → max-1500 chars to match the canonical seam; drop redundant "What You Don't Do" (subsumed by Contract) and surface a trimmed `## Scope Discipline` rump that names co-agent role boundaries (Age owns quality, age-spec owns compliance, Cook owns the fix); add cross-harness fallback note. Lifts press 65 → 80, Permission model per stage 70 → 75 (writer-trio now complete; only compiler propagation remains as blocked-on-src), and Compaction seams 70 → 75 (all four writer/scout agents now share the same 1500-char + $TMPDIR seam).
- Iteration 14: rewrite `commands/debug.md` (Flow 3 entry) with a concrete six-step **Execution protocol** that turns the previously-gestural "Dispatch contract" into actionable orchestration — explicit `Agent(subagent_type=culture|cook|press, ...)` invocations with full prompts spelled out (Culture's confidence-≥-50 root-cause gate, Cook's "edit only Culture-cited files" hard constraint, Press's "regression test that would have failed pre-fix" obligation), explicit `Skill(skill="age", args="--no-fix --scope ...")` for the Age step (--no-fix prevents /age from double-spawning its own Press loop), `AskUserQuestion`-gated step-1 confirmation and step-6 fix-loop continuation, three-loop convergence cap with explicit loop-counter tracking, kebab-case `<slug>` derivation rule that ties every $TMPDIR path together, and a Hand-off contract table enumerating the exact agent-declared paths (`$TMPDIR/fromage-{culture,cook,press}-<slug>.md` + `$TMPDIR/age-<slug>.md`). Adds a Cross-harness portability section naming Codex (no Agent tool — sequential turns), Copilot CLI (no parallel spawn — already matches sequential handoff), Cursor (no per-agent allowlist — prompt-only enforcement on Culture). Trims the Deferred section to two truly blocked-on-src items (compiler does not propagate disallowedTools/permissionMode into rendered frontmatter; no durable loop-counter file). Lifts Flow 3 60 → 85 and proves the dispatch-wiring pattern that the next iteration can replicate to Flows 1, 2, 4, 5, 6.
- Iteration 15: replicate the iter-14 Execution-protocol pattern to `commands/fromage.md` (Flow 1 / Spec-First). Surfaces and corrects a long-standing canonical-vs-agent role mismatch — the canonical doc labels stages `Cook → Cut → Press → Age` with pipeline-phase semantics (plan → decompose → execute → review), but the agent ecosystem assigns TDD-flavored roles (Cook = implement, Cut = red-tests pre-impl, Press = adversarial post-impl). The actual sub-agent dispatch order under the agent semantics is `Cut → Cook → Press → Age`, which the rewrite makes explicit via a phase→agent mapping table in the Flow section. Step 1 unifies resolve + validate (Approach + Quality gates) + classify (>= 4 atoms → `/fromagerie`; linear backlog → `/incremental`) + slug derivation + plan write to `$TMPDIR/fromage-<slug>-plan.md` + `AskUserQuestion` gate. Steps 2-4 are explicit `Agent(subagent_type=cut|cook|press, ...)` invocations with full prompts pinning per-stage scope (Cut: pin only spec-named obligations, no invented edge cases; Cook: edit only plan-named files, no test edits; Press: tests-only post-impl, score 0-100). Step 5 is `Skill(skill="age", args="--no-fix --scope ...")` so the Fromage orchestrator owns the fix loop. Step 6 branches success / impl-only fix loop back to Cook / spec-invalidation halt to `/mold`, with the three-loop convergence cap. Hand-off contract enumerates every $TMPDIR path; Cross-harness portability section names Codex / Copilot CLI / Cursor degradation paths. Lifts Flow 1 60 → 85.
- Iteration 16: replicate the iter-14/15 Execution-protocol pattern to `commands/pr-finish.md` (Flow 4 / PR-Finish). Surfaces and corrects the same canonical-vs-agent role mismatch — the canonical doc labels the flow `Culture → Cut → Press → Age` with Cook-skipped pipeline semantics where "Press executes the task list," but our `press.md.eta` is tests-only post-impl. The rewrite re-explains "Cook skipped" as "no fresh planning Cook" (the architectural decision lives in the existing PR description) while making the actual sub-agent dispatch order `Culture → Cut → Cook → Press → Age` explicit via a phase→agent mapping table; the cook sub-agent IS dispatched at step 4 to do the remaining implementation work, and the canonical-doc Cut/Press approval gate shifts to between Cut and Cook under agent semantics. Step 1 unifies PR/branch resolve + classify + slug derivation (`pr-<number>` or kebab branch) + plan stub write to `$TMPDIR/pr-finish-<slug>-plan.md` + `AskUserQuestion` gate. Steps 2-5 are explicit `Agent(subagent_type=culture|cut|cook|press, ...)` invocations with full prompts pinning per-stage scope (Culture: confidence-≥-50 divergence-assessment gate plus required goal/diff-surface/CI-failures/open-threads/linked-spec deliverables; Cut: pin only PR/spec/thread-named obligations + each failing CI check is a contract claim; Cook: edit only Culture's diff surface, no rebase/force-push; Press: tests-only post-impl, score 0-100). Approval gate fires *inside* step 3 with three-choice menu (confirm/edit/abort). Step 6 is `Skill(skill="age", args="--no-fix --scope ...")` so the PR-Finish orchestrator owns the fix loop. Step 7 has three exit branches (success / impl-only fix loop back to Cook / divergent-PR halt to `/mold`) plus the three-loop convergence cap. Hand-off contract enumerates every $TMPDIR path; Cross-harness portability section names Codex / Copilot CLI (gh-pr surface is Copilot-native) / Cursor degradation paths. Lifts Flow 4 60 → 85.
- Iteration 17: replicate the iter-14/15/16 Execution-protocol pattern to `commands/explore.md` (Flow 2 / Exploration) — the unique flow with an iterative Culture↔Cook loop bracketing the approach lock. Adds an explicit phase→agent mapping table that resolves the role layering — the canonical `Culture → Cook iterative → Cut → Press → Age` becomes the loop (`Culture → Cook` × up to 3) → approach lock → `Cut → Cook → Press → Age`, with Cook used twice (propose-only during the loop, standard impl mode after the lock). Step 1 unifies classify + redirect (six target flows) + slug derivation + `AskUserQuestion` gate. Step 2 is the Culture↔Cook loop with explicit per-cycle `Agent(subagent_type=culture)` and `Agent(subagent_type=cook, ...propose-only...)` invocations, cycle-counter tracking starting at 1, capped at 3, and a Cook-driven branch decision (`advance to lock` vs `loop again` with focusing question). Step 3 is the `AskUserQuestion`-gated approach lock with the three-choice menu and a stable `$TMPDIR/explore-<slug>-approach.md` proposal path. Steps 4-6 are explicit `Agent(subagent_type=cut|cook|press, ...)` invocations against the locked approach (Cook impl mode here is the standard production-code writer; the lock is binding so rejected alternatives stay rejected; Press's primary attack surface is the locked approach's "unresolved questions"). Step 7 is `Skill(skill="age", args="--no-fix --scope ...")`. Step 8 has three exit branches (success / impl-only fix loop back to Cook / approach-invalidation halt to `/explore` or `/mold`) plus the three-loop convergence cap. Hand-off contract enumerates every $TMPDIR path with explicit namespace separation (`explore-<slug>-*` for loop artifacts, `fromage-*-<slug>` for post-lock pipeline). Cross-harness portability section explicitly notes loop-Cook's propose-only constraint is prompt-only on every harness. Deferred section lists three blocked-on-src items including a missing first-class `cook-propose-only` flag in source frontmatter. Lifts Flow 2 60 → 85; only Flow 6 and Flow 7 remain at 60 among the flows.
- Iteration 18: replicate the iter-14/15/16/17 Execution-protocol pattern to `commands/incremental.md` (Flow 6 / Incremental) — the unique flow whose four-stage pipeline loops × N tasks. Adds an explicit phase→agent mapping table that resolves the canonical-vs-agent role layering — the canonical `[Cook task-N → Cut → Press → Age] × N` becomes per task `[orchestrator: write task-N plan] → Cut → Cook → Press → Age` under agent semantics, with the planning step folded into the orchestrator (Cook is dispatched only once per task in its standard production-code role; Cook-as-planner per task would have violated its single-purpose Permission Contract). Step 1 unifies spec resolve + classify + redirect (four sibling flows: `/fromagerie` for parallel atoms, `/debug` for bugs, `/explore` for fuzzy problems, `/pr-finish` for partial branches, `/mold` for missing Approach + Quality gates) + slug derivation + state-file init or resume from first non-`done` task + `AskUserQuestion` gate. State-file shape is now spelled out as concrete JSON with task index, status (`pending`/`in-progress`/`done`/`failed`/`skipped`), `fix_loop_attempts` counter, plan-path pointer, and spec-line range. Step 2 is the per-task pipeline as an explicit seven-substep loop (2a orchestrator plan write, 2b Cut, 2c Cook, 2d Press, 2e Age, 2f per-task fix-loop branch decision, 2g cross-task hand-off) with full `Agent(subagent_type=cut|cook|press, ...)` prompts pinning per-task scope (Cut: pin only task-N's named obligations, do not pull work forward from later tasks; Cook: do NOT touch files reserved for tasks > N, no test edits; Press: scope attack surface to task-N's production diff only, do not chase prior-task regressions; Age: scoped to task-N's net diff). Step 2e is `Skill(skill="age", args="--no-fix --scope ...")` so the Incremental orchestrator owns the per-task fix loop. Step 2f has four branches (success → mark `done` + advance / impl-only fix loop back to Cook / plan-invalidation halt to `/mold` / three-loop cap → mark `failed` + halt). Step 3 returns the cumulative summary when the backlog (or requested task range) is complete. Hand-off contract enumerates every per-task $TMPDIR path with explicit `incremental-<slug>-task-<N>-*` namespace separation, distinct from the canonical `fromage-*-<slug>` namespace so per-task artifacts stay isolated and resume-friendly. Cross-harness portability section names Codex / Copilot CLI / Cursor degradation paths and explicitly calls out that the JSON state file is harness-agnostic — any harness can read and resume against it. Deferred section lists three blocked-on-src items (compiler propagation; per-task scope enforcement is prompt-only without a `--scope` flag on cook/press source frontmatter or a per-task worktree; task-cycle counter is mostly closed by the durable state file). Lifts Flow 6 60 → 85; only Flow 7 (Learn) remains at 60 among the flows.
- Iteration 19: replicate the iter-14/15/16/17/18 Execution-protocol pattern to `commands/culture.md` (Flow 7 / Learn) — the unique flow whose path is a single sub-agent stage. Adds an explicit phase→agent mapping table that resolves the canonical-vs-agent role layering — the canonical `Culture (only)` (or optional `Culture → Cook (draft explanation)`) becomes `Culture (only)` under agent semantics, with the canonical-doc "Cook draft explanation" folded into an orchestrator-side aggregation step (since cook's production-code-only Permission Contract is incompatible with docs-only output; dispatching Cook in the Learn flow would violate its single-purpose Permission Contract). Step 1 unifies classify + redirect (eight sibling flows: `/debug` for bugs, `/explore` for fuzzy + build, `/mold` for spec-only, `/fromage` for known specs, `/pr-finish` for branches, `/briesearch` for external libs, `/age` for review) + slug derivation + `AskUserQuestion` gate. Step 2 is the per-cycle `Agent(subagent_type=culture, ...)` invocation with cycle-counter tracking starting at 1, capped at 3, full prompt pinning per-cycle scope (deep understanding for a human reader, NOT problem diagnosis; the report IS the deliverable since there is no downstream Cook/Cut/Press/Age; required findings: 3-7 essential entry points, execution flow, architectural pattern to internalize, "if you understand this, you can do X" paragraph, open sub-questions for next cycle), and an explicit `$TMPDIR/learn-<slug>-culture-c<N>.md` path override that namespace-isolates Learn artifacts from any concurrent `/explore` or `/debug` run on the same topic (which would otherwise default to `fromage-culture-<slug>.md`). Step 3 reads all completed cycle reports, composes an aggregated learn summary at `$TMPDIR/learn-<slug>-summary.md`, then offers `AskUserQuestion` with three branches (understanding sufficient → exit / loop another aspect with focusing question — capped at 3 cycles / discovery → halt + redirect to `/explore` / `/mold` / `/fromage` / `/debug` / `/pr-finish`). Step 4 returns the structured learn summary as the canonical artifact with explicit "NEVER spawn cook/cut/press/age" and "NEVER recommend `/culture` recursively" guardrails (depth comes from branch 2 inside Step 3, not a fresh session). Hand-off contract enumerates every per-cycle $TMPDIR path with explicit Learn-namespace isolation. Cross-harness portability section names Codex (cycles → sequential turns) / Copilot CLI (sequential native) / Cursor (no allowlist → prompt-only Permission Contract on culture) degradation paths and explicitly calls out that the Learn flow is uniquely exposed to the compiler-propagation gap because Culture is its only stage (no downstream agent could catch a Culture-side production write). Deferred section limited to two blocked-on-src items (compiler propagation, cycle-counter persistence). Lifts Flow 7 60 → 85 — completes the seven-flow Execution-protocol replication; every flow except Flow 5 (Review) is now at 85.
- Iteration 20: replicate the iter-14..19 Execution-protocol pattern to `commands/age.md` (Flow 5 / Review) — the final flow not yet at 85, completing the seven-flow gold-standard sweep. Surfaces and corrects the canonical-vs-agent role mismatch — the canonical doc labels the loop `Press → Age (re-review)` with Press-as-fixer semantics, but our `press.md.eta` is tests-only post-impl. The rewrite re-explains the fix loop as `Cook (production fix) → Press (regression tests, optional) → age skill (re-review)` via an explicit phase→agent mapping table; Cook is the primary fixer because most Age findings (bugs, complexity, leaks, dead code, spec drift) are production-code issues that Press's tests-only Permission Contract forbids; Press is dispatched only when an Age finding is `SPEC_MISSING` shaped (regression-test missing). Step 1 unifies target resolve + classify (redirect to `/debug` for bugs, `/pr-finish` for stalled PRs, `/mold`+`/fromage` for fresh features, `/culture`+`/explore` for no-diff exploration) + slug derivation (`pr-<number>` for PRs, kebab branch for branches, last path component for paths) + `AskUserQuestion` gate. Step 2 is the Culture pre-pass with explicit `Agent(subagent_type=culture, ...)` invocation, an explicit `$TMPDIR/age-<slug>-context.md` path override that namespace-isolates the Review brief from any concurrent `/debug` or `/fromage` Culture run on the same target, and required goal/diff-surface/CI-failures/open-threads/linked-spec/prior-Age-reports deliverables plus a confidence-≥-50 reviewability gate. Step 3 invokes the `age` skill (which spawns the six dimensions in parallel and merges with history modifiers) and writes the unified report to `$TMPDIR/age-<slug>.md`. Step 4 is the fix-loop branch decision with three `AskUserQuestion` choices (confirm fix / abort / spec-invalidation halt). Step 5 is `Agent(subagent_type=cook, ...)` scoped to Age-cited files only with full prompt pinning per-loop scope. Step 6 is the optional `Agent(subagent_type=press, ...)` dispatch — invoked only if any Age finding is `SPEC_MISSING` shaped. Step 7 is `Skill(skill="age", args="--no-fix --scope ...")` for delta-only re-review, with three exit branches (success / loop again with counter increment / three-loop convergence cap halt) plus a spec-invalidation override that halts regardless of loop count. Hand-off contract enumerates every $TMPDIR path with explicit namespace separation (`age-<slug>-context.md` for Culture brief override, `age-<slug>.md` for initial review, `age-<slug>-loop-<N>.md` for re-review per loop, `fromage-cook-<slug>.md` and `fromage-press-<slug>.md` for the canonical writer seams). Cross-harness portability section names Codex (Agent → sequential turns; age skill's six-dimension parallel-spawn → sequential on Codex) / Copilot CLI (sequential outer flow native, age-skill parallelism degrades) / Cursor (no allowlist → prompt-only enforcement on Culture + six dimensions) degradation paths. Deferred section lists three blocked-on-src items (compiler propagation, loop counter persistence, first-class `--classify` flag on the age skill that returns `production_fix` / `regression_test_only` buckets). Lifts Flow 5 65 → 85; with this iteration **every flow now scores 85** — the seven-flow Execution-protocol sweep is complete.
